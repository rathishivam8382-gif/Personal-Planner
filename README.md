# Personal-Planner

Week Planner — "LifeOS". A single-file React planner (tasks, notes, focus, analytics).

## Run locally
```bash
npm install
npm run dev
```

## Build
```bash
npm run build
```
Output is in `dist/`. Deploys to Vercel as a Vite app (auto-detected).
